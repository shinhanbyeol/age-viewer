{
  "name": "ag-viewer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "setup": "npm install && npm-run-all setup-front setup-backend",
    "setup-front": "cd frontend && npm install",
    "setup-backend": "cd backend && npm install",
    "front": "cd frontend && npm run start",
    "build-front": "cd frontend  && npm install && npm run build",
    "build-back": "cd backend  && npm install && npm run build",
    "backend": "cd backend && npm run start",
    "deploy": "npm-run-all build-front backend",
    "bundle": "cd backend && webpack --config webpack.config.js && cd .. && mkdir -p dist/frontend && cp -r frontend/build  dist/frontend/build && cp -r backend/misc dist && cp -r backend/sql dist",
    "start": "npm-run-all --parallel backend front"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "devDependencies": {
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  },
  "license": "Apache-2.0",
  "licenses": [
    {
      "type": "Apache-2.0",
      "url": "https://www.apache.org/licenses/LICENSE-2.0"
    }
  ]
}
